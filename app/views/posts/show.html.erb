<% content_for :title, @post.title + ' | GyozaGuy.com' %>
<div class="post">
	<h3><%= @post.title %></h3>
	<h4><%= @post.post_date %> | By <%= link_to @post.user.username, @post.user %></h4>
	<%= @post.content.html_safe %>
</div>
<%= render partial: 'comments/comment' %>